apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: mount
  name: test-pod
spec:
  containers:
  - command:
    - sleep
    - 999h
    image: alpine
    name: mount
    resources: {}
    volumeMounts:
      - name: test-volume
        mountPath: /mypv
  dnsPolicy: ClusterFirst
  restartPolicy: Always
  volumes:
    - name: test-volume
      persistentVolumeClaim:
        claimName: my-pvc
status: {}
